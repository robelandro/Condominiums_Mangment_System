package helper;

public class Guest extends Person{
    public Guest(String firstName, String lastName, int age, String address, String sex) {
        super(firstName, lastName, age, address, sex);
    }
}
